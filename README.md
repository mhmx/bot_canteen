# Lunch Poll Bot - Телеграм бот для опросов о походе в столовую

Телеграм бот для создания ежедневных опросов в групповом чате о походе в столовую в заданное время.

## Возможности

- 📊 Автоматическая отправка опроса в указанное время в групповом чате Telegram
- 🕒 Проверка будних дней и праздников (использует API isdayoff.ru)
- 📝 Логирование всех действий бота в файл `bot.log`
- 🔄 Поддержка повторных попыток отправки опроса при ошибках
- 🗳 Опрос с вариантами ответа: "Да", "Нет", "Другое время"

## Установка и настройка

### 1. Установка зависимостей

Установите необходимые Python библиотеки:

```bash
pip install -r requirements.txt
```

### 2. Настройка конфигурации

Создайте или отредактируйте файл `config.py` и укажите ваши настройки:

```python
# Конфигурация бота
TOKEN = 'your_bot_token_here'  # Токен вашего Telegram бота
GROUP_CHAT_ID = 'your_group_chat_id_here'  # ID группового чата
TIMEZONE = 'Europe/Moscow'  # Часовой пояс (например, 'Europe/Moscow')
SEND_TIME = '10:00'  # Время отправки опроса (например, '10:00')
GO_TIME = '12:00'  # Время похода в столовую (например, '12:00')
```

### 3. Запуск бота

Запустите бота с помощью команды:

```bash
python bot.py
```

## Структура проекта

```
lunch_poll_bot/
├── bot.py                  # Основной файл бота
├── config.py               # Конфигурация приложения
├── bot.log                 # Лог-файл с действиями бота
├── README.md               # Документация
└── requirements.txt        # Зависимости Python
```

## Использование

Бот автоматически отправляет опрос в указанное время (`SEND_TIME`) в групповой чат Telegram с вопросом:  
"Идем в столовку в {GO_TIME}?"  

Варианты ответа:
- Да
- Нет
- Другое время  

Опрос отправляется только в будние дни, если день не является праздником (проверка через API isdayoff.ru). Все действия бота логируются в файл `bot.log`.

## Зависимости

Бот использует следующие технологии:
- **pyTelegramBotAPI** - для работы с Telegram Bot API
- **schedule** - для планирования ежедневных задач
- **requests** - для проверки праздничных дней через API
- **pytz** - для работы с часовыми поясами

### Пример requirements.txt

```text
pyTelegramBotAPI==4.22.1
schedule==1.2.2
requests==2.32.3
pytz==2024.2
```

## Логирование

Все действия бота записываются в файл `bot.log` с временной меткой в формате `[YYYY-MM-DD HH:MM:SS]`. Логи также выводятся в консоль для удобства отладки.

## API

Бот использует внешний API [isdayoff.ru](https://isdayoff.ru/) для проверки, является ли текущий день праздником в России.

## Лицензия

MIT License